site_name: coninstruction
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: coninstruction
      handlers:
        python:
          options:
            docstring_style: numpy